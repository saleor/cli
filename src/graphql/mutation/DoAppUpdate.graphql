mutation AppUpdate($app: ID!, $permissions: [PermissionEnum!]) {
  appUpdate(id: $app, input: { permissions: $permissions }) {
    app {
      id
    }
    errors {
      field
      message
    }
  }
}
